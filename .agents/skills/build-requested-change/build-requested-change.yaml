orchestrator: build-requested-change
intent: develop requested functionality or a technical change
inputs:
  - requested change
stages:
  - scope-feature classifies the change and derives applicable stages
  - deliver-change owns every delivery
human_decisions:
  - approve durable specifications before implementation unless YOLO applies
outcomes:
  - released change
  - specification awaiting approval
  - blocker
