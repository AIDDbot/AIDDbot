orchestrator: build-requested-change
intent: develop requested functionality or a technical change
inputs:
  - requested change
stages:
  - scope-feature
  - deliver-spec for one specification
  - deliver-change for coordinated specifications
human_decisions:
  - approve each specification before implementation unless YOLO applies
outcomes:
  - released change
  - specification awaiting approval
  - blocker
