# PRD — {Product_Name}

{Product problem and goals, functional level — one short paragraph; grow it as specs accumulate.}

> Index of specs grouped by feature area — the product's functional map.
> Append-only; written only by `/specify`. Status lives in each spec's frontmatter.

## {Feature_Area}

- **{NNN}** [{slug}](./{NNN}-{slug}/spec.md) — {one-line outcome}.
- **{NNN}** [{slug}](./{NNN}-{slug}/spec.md) — {one-line outcome}. Supersedes {NNN}.
