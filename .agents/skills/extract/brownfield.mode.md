# Extract — Brownfield mode

Describe the dominant patterns from existing code.

## `{tier}.rules.md`

- [ ] glob file names → classify by artifact role (naming + content heuristics). Read 1-2 representative files per role for the dominant pattern. Detect deviations by skimming structurally different files; flag low-confidence inferences. Use one real (trimmed) snippet as the canonical example.
