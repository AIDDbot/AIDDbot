# Scaffold report — {back-tech} + {front-tech} + {e2e-tech} ({domain})

Date: {ISO date}

## Pieces

| Piece | Status (`fetched` / `kept` / `refreshed` / `skipped`) | Source ref | Toolchain (pm, port, commands) |
|---|---|---|---|
| `back/` | | | |
| `front/` | | | |
| `e2e/` | | | |
| `docs/domain/` | | | |
| `.agents/` | | `AIDDbot/AIDDbot/.agents` or `n/a` | editor folders copied / skipped |

## Reconciliation

- {fix applied}

## Tracer bullet

- Result: `pass` | `fail`
- {error if fail}

## Pending decisions

- {hooks, ports, anything ambiguous}
