# PRD — {Product_Name}

{Product problem and goals, functional level — one short paragraph; grow it as specs accumulate.}

> Functional log — created by `/explore`. Specs indexed by category when `/specify`
> creates them — append-only. Status lives in each spec's frontmatter.

<!-- Example entry shape (do not invent categories at explore time):

## {category}
{short line description of the category}
### [{spec_id} {title}](./{spec_key}/spec.md)

  - **Tags**: `{tag1, tag2}`

-->
