# PRD — {Product_Name}

{Product problem and goals, functional level — one short paragraph; grow it as specs accumulate.}

> Functional log — created by `/explore`. Specs indexed by feature area when `/specify`
> creates them — append-only. Status lives in each spec's frontmatter.
