# Excavate — Greenfield mode

Prescribe structure and contracts from model knowledge and ecosystem best practices.

## `{tier}.arch.md`

- [ ] prescribe code organization (layer/feature/hybrid), components, shared artifacts, key contracts, and storage — consistent with ADRs. Ground it in the stack's conventions (official docs, scaffolding defaults, a reference architecture) and **name that source** so `/extract` and `/codify` can follow it; use model priors only when no convention exists. Propose dev commands (init, build, run, test, lint) even if folders don't exist yet.

## `ER.md`

- [ ] model entities, fields, constraints, and relationships from the product brief and prescribed tiers.
